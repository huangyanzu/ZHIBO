//
//  Common.swift
//  CMTV
//
//  Created by aZu on 2021/1/5.
//

import UIKit

let KStatusBarHeight : CGFloat  = UIScreen.main.bounds.size.height >= 780 ? 44 : 20

let KNavigationHeight : CGFloat = UIScreen.main.bounds.size.height >= 780 ? 88.0 : 64.0

let KTabBarHeight : CGFloat = UIScreen.main.bounds.size.height >= 780 ? 83.0 : 49.0

let KSafetyZoneHeight : CGFloat = UIScreen.main.bounds.size.height >= 780 ? 34 : 0

let kScreenHeight  = (CGFloat)(UIScreen.main.bounds.size.height)

let kScreenWidth  = (CGFloat)(UIScreen.main.bounds.size.width)

let ScalePpth  = (CGFloat)((kScreenWidth / 375.0))

